import { Image, Text, TextInput, TouchableOpacity, View } from 'react-native';
import { styled } from 'nativewind';

const TWText = styled(Text);
const TWView = styled(View);
const TWTextInput = styled(TextInput);
const TWImage = styled(Image);
const TWTouchableOpacity = styled(TouchableOpacity);

export { TWText, TWView, TWTextInput, TWImage, TWTouchableOpacity };
